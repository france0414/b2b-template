# Custom Components Zone

Client-specific presentation components and variants.

Examples:

- hero variants
- brand story sections
- application-focused showcase blocks

Rules:

- Can consume fixed components.
- Cannot become a dependency of fixed components.
- Keep data integration at adapter/page level, not inside visual blocks.
