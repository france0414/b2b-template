# Fixed Components Zone

Shared components that must remain stable across clients.

Examples:

- header/footer
- section wrappers
- product/article cards
- shared CTA/form shells

Rules:

- No client-specific business copy hardcoded.
- No direct dependency on custom components.
- Must accept typed props and be independently reusable.
